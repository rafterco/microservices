server:
  port: 8080
spring:
  application:
    name: customer
  datasource:
    username: 'colinrafter'
    url: jdbc:postgresql://localhost:5433/customer
    password: 'password'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: update
    show-sql: 'true'